name: picofaz12folin-1

on:
  workflow_dispatch:

  schedule:
    - cron: "0 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Install requests
        run: python -m pip install requests

      - name: Test Divar
        run: python d.py
        run: python m.py
